#include<iostream>
using namespace std;
int main()
{
	int num,total=0;
	{
		do
		{
			cout<<"enter a number (o to quit);";
			cin>>num;
			if(num==0)
			{
				cout <<"end of data entry "end;
				break;
			}
			if(num<0)
		}
		cout<<"skipping this no."<<end;
		
		continue:
	}
	total+=num;
}
while(1);
cout<<"total of +av number is"<<total;
}
